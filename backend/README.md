# AccountEase API

Spring Boot REST API foundation for the AccountEase ledger. Configure `DATABASE_URL`, `DATABASE_USERNAME`, and `DATABASE_PASSWORD`, then run `mvn spring-boot:run`. `schema.sql` creates the owner, organization, customer, and transaction tables and seeds the demo owner (`rajesh` / `demo123`).